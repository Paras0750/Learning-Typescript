let user = { name: "Paras", age: 20 };

console.log("Paras");
console.log(user.name);

//Typescript is a superset of javascript which means that all javascript code is valid typescript code
// Typescript is a strongly typed language which means that we have to define the type of a variable when we declare it
// Typescript is used to develop large(scaleable) applications
// Typescript code is converted to javascript code using tsc command
